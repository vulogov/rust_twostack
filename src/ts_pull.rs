use crate::ts::TS;
// use crate::stack::Stack;
// use rust_dynamic::value::Value;

impl TS {
    // pub fn current(&mut self) -> &mut Stack<Value> {
    //     match self.stack.peek() {
    //         Some(curr) => curr,
    //         None => {
    //             self.add_stack();
    //             return self.current();
    //         }
    //     }
    // }
}
